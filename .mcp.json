{
  "mcpServers": {
    "tessera": {
      "command": "uv",
      "args": [
        "--directory", "/Users/danieliser/Toolkit/codemem",
        "run",
        "mcp-hmr", "tessera.server:app"
      ],
      "env": {
        "TESSERA_EMBEDDING_ENDPOINT": "http://localhost:8800/v1/embeddings",
        "TESSERA_EMBEDDING_MODEL": "nomic-embed"
      }
    }
  }
}
